# deps
node_modules/

# kane runtime artifacts (sealed evidence packs live in .testmuai/, sessions in ~/.testmuai/)
.testmuai/
.kane.lock/
.kane-stderr.log

# env & secrets
.env
.env.*

# large recordings (key evidence is copied into evidence/, big videos are linked)
*.mp4
*.mov

# editor/os noise
.DS_Store

# scratch from visual verification
.playwright-mcp/

# runtime event log (hooks write it; demo-reset truncates it)
.kane-events.ndjson

# private working docs — strategy/build notes, not part of the submission
docs/kane-loop-build-guide.md
docs/kane-loop-winning-strategy.md
docs/kane-loop-master-prompt.md

# local demo run sheet + submission text
DEMO-AND-SUBMISSION.md

# oracle seal — per-session state, not a committed artifact
.kane-oracle.sha256

# demo film: capture, composition and renders — local build artifacts
demo/
